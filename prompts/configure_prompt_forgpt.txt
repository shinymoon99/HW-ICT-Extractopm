配置接口允许通过的VLAN，取值范围是1～4094。
* 配置BGP对从全局接收的路由进行过滤
* 配置simple加密方式认证。
配置设备与控制器建立连接时使用的对接密码。
配置接口加入的缺省VLAN，取值范围为1～4094。
<t>配置</t><o>Trunk类型接口</o>的<p>缺省VLAN</p>，取值范围是<r>1～4094</r>。
3. 配置Keychain认证。
为模板配置AS接口入方向上DHCP报文的限速值。
4. <t>配置</t><o>接口</o>的<p>链路类型</p>为<v>Hybrid</v>。